# ==============================================================
# Contains configuration overrides needed for running tests
# ==============================================================
# Use H2
spring.jpa.database=h2
# Re-creates DDL on app start and drops it on app stop
spring.jpa.hibernate.ddl-auto=create-drop
